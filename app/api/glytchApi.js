"use strict";

class GlytchApi {
  static loadAllImages() {
    return new Promise((resolve, reject) => {
      
    });
  }

  static uploadImage(image) {
    return new Promise((resolve, reject) => {
      resolve(Object.assign({}, image));
    });
  }
}

export default GlytchApi;
